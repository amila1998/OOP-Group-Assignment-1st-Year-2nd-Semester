package OMTpkg.advertistments;

import java.io.IOException;
import java.util.List;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import OMTpkg.videos.video;
import OMTpkg.videos.videoDBUtil;

/**
 * Servlet implementation class advertistmentUpdateServlet
 */
@WebServlet("/advertistmentUpdateServlet")
public class advertistmentUpdateServlet extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
	/**
	 * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		String uid = request.getParameter("uid");
		String ad_id = request.getParameter("ad_id");
		
		String ad_name = request.getParameter("ad_name");
		String period = request.getParameter("period");
		String adCategory = request.getParameter("adCategory");
		
		boolean istrue
		
		
		advertiDBUtil.updatevideoDetails(vid,title, description, ageRestriction, category);
		
		String UID=String.valueOf(uid);
			
			List<video> myvideos = videoDBUtil.getMyvideos(UID);
			request.setAttribute("myvideos", myvideos);
			
			RequestDispatcher dis = request.getRequestDispatcher("myvideos.jsp");
			dis.forward(request, response);
		
		
	}

}
